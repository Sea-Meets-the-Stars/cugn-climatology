# Download the data

This prompt doc will download the data for the California Underwater Glider Network (CUGN) climatology.

## Process

If you need to run Python, use the "ocean14" conda environment.

## Context 

See the code in the `cugn` repository for how to download the data.
A local copy is next to this Repo.

The new CUGN climatology generated by the project is located at: 
`https://spraydata.ucsd.edu/products/cugn-climatology/new.php`.  
I will refer to it as `2026 beta`.

## Prompts

1, Execute the first task under Grab the data.

## Grab the data

1. This webpage -- `https://spraydata.ucsd.edu/products/cugn-climatology/new.php` -- under Data Access has a list of NetCDF files.  Please download the files to the `$OS_SPRAY/CUGN/Climatology/2026 beta` directory.  If you have any questions, please ask in the Q&A section below.  Log your work.

## Prompts for Claude Science

1. I have just downloaded the 30 CUGN climatology NetCDF files from   
https://spraydata.ucsd.edu/products/cugn-climatology/ and put them on this laptop in the $OS_SPRAY/CUGN/Climatology directory.  I wish for you to explore these data and report on your findings.  You are encouraged to ask me questions as we go.   Please ask them here and also log them in the file Oceanography/python/cugn/claude_prompts/prep_for_science.md.  Also log your work in the Logs section of that file.

## Q&A

## Logging

Append an entry to the **Logs** section of this file using the format:

```
### <Date> (Short summary of the work)

<Detailed description of the work and what you learned>
```

## Logs

### 2026-07-15 (Downloaded the 2026 beta climatology NetCDF files)

Downloaded all 80 NetCDF files listed under Data Access on
https://spraydata.ucsd.edu/products/cugn-climatology/new.php to
`$OS_SPRAY/CUGN/Climatology/2026 beta/` (5.9 GB total).

Details:

- Wrote the download as a repository script:
  `cugn_climatology/download_2026beta.py` (run with
  `python -m cugn_climatology.download_2026beta` in the `ocean14` env).
  It scrapes the page, strips HTML comments, extracts the `.nc` links, and
  downloads each file, skipping any that already exist.
- The live Data Access listing contains **80** files, not 30: 30 long-term
  files (`lt_*`, Lines 66/80/90) and 50 short-term files (`st_*`, Lines
  56/66/80/90 plus an along-shore `al` section), each in depth and sigma
  (potential density) vertical coordinates.
- The page's raw HTML also contains the old 30-file listing
  (mean/ancycle/anomaly/mean_ancycle/total × z/rho × Lines 66/80/90), but it
  is commented out on the page, so those files were NOT downloaded.
- Verified the download: 80 files on disk, and `lt_mean_depth_90.nc` opens
  cleanly with xarray (dims depth=50, distance=107; variables include
  temperature, salinity, chlorophyll_a, sigma_t, potential_density,
  geostrophic_velocity, doppler velocities).
