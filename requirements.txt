# Runtime dependencies for cugn_climatology.
#
# Keep this list in sync with install_requires in setup.py.

# Core scientific stack
numpy
scipy
pandas
matplotlib
seaborn
scikit-learn
tqdm
IPython

# Oceanography / gridding
xarray
h5netcdf
netcdf4
cftime
gsw
gsw-xarray
xhistogram
dask

# Sibling package (not on PyPI): CUGN glider data handling and analysis.
# Install from the local checkout alongside this repository (see CLAUDE.md):
#   pip install -e ../cugn
